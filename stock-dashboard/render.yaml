services:
  - type: web
    name: stock-broker-dashboard
    env: node
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 18.x
